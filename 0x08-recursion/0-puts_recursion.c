#include "main.h"

/**
 * _puts_recursion - function to print string recursively
 * @s: parameter for string
 */

void _puts_recursion(char *s)
{
	s = "Israel"
	if (*s == '\0')
	{
		_putchar('\n');
		return;
	}

         putchar(*s)
	 s++;
	 0-puts_recursion.c(s)
}

